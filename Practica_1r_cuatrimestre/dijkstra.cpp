#include <iostream>
#include "leftistHeap.h"
#include "WDGraph.h"

int main() { 

    // LeftistHeap h; 
    // LeftistHeap h1;
  
    // h.insert(13); 
    // h.insert(30); 
    // h.insert(58); 
    // h.insert(25); 
    // h.insert(71); 
    // h.insert(93); 
    // h.insert(64); 
    // h.insert(43); 
    // h.insert(23); 
    // h.insert(98); 
    // h.insert(12); 

    // h1.insert(43);
    // h1.insert(23); 
    // h1.insert(76); 
    // h1.insert(87); 
    // h1.insert(16); 
    // h1.insert(81); 
    // h1.insert(12); 
    // h1.insert(54); 
    // h1.insert(32); 

  

    // h.printHeap();
    // h1.printHeap();

    // h.decreaseKey(43, 16);
    // h1.decreaseKey(23, 78);

    // h.printHeap();
    // h1.printHeap();   

    // WUGraph g1;
    // g1.addEdge(0, 1, 3);
    // g1.addEdge(0, 2, 5);
    // g1.addEdge(2, 1, 2);
    // g1.addEdge(0, 3, 1);
    // g1.addEdge(3, 4, 7);
    // g1.addEdge(4, 5, 6);
    // g1.addEdge(1, 5, 3);
    // g1.addEdge(8, 9, 6);
    // g1.addEdge(23, 45, 3);
    // g1.printGraph();
  
    return 0; 
} 